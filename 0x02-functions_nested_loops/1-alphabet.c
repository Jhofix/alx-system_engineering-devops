#include "main.h"

/**
 * print_alphabet - short descritpio 
 *
 * Description: main description
 *
 * Return: Always return 0
 */

void print_alphabet(void);
{
	char c;

	for (c = "a", c <= "z", c++)
	{
		_putchar(c);
	}
	return (0);
}
